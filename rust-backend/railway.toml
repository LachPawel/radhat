[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Temporarily disable healthcheck to see startup errors
# healthcheckPath = "/health"
# healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
